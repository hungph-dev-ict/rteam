version: '3.8'

services:
  hirec-automation:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
